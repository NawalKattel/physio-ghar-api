NODE_ENV=development
PORT=3000

# Matches docker-compose.yml (db service)
DB_HOST=localhost
DB_PORT=5432
DB_USER=physioghar
DB_PASSWORD=physioghar
DB_NAME=physioghar
DB_LOGGING=false

# At least 32 characters: openssl rand -base64 48
JWT_ACCESS_SECRET=change-me-to-a-random-string-of-32-plus-chars
JWT_ACCESS_TTL=900
REFRESH_TOKEN_TTL_DAYS=30
